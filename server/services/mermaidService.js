/**
 * Construit une structure intermédiaire à partir des fichiers analysés.
 * @param {object[]} files
 * @returns {object}
 */
export const buildStructure = (files) => {
};

/**
 * Rend la structure en syntaxe Mermaid (string).
 * @param {object} structure
 * @returns {string}
 */
export const render = (structure) => {
};
